# Alcove Dux local API state.
ALCOVE_DUX_DB_PATH=/app/state/alcove_dux.sqlite

# Hosted-mode Docker overlay placeholders. Replace before exposing any service.
POSTGRES_DB=alcove_dux
POSTGRES_USER=alcove_dux
POSTGRES_PASSWORD=change-me-for-local-dev

# Optional service ports for local experiments.
ALCOVE_DUX_API_PORT=8000
CHROMA_PORT=8001
